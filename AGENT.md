# AWS Pinpoint Email CLI - Agent Guide

Command-line interface for AWS Pinpoint Email.

## Installation
```bash
npm install -g @ktmcp-cli/amazonpinpoint
```

## Configuration
```bash
pinpoint-email config set --api-key YOUR_KEY
```

## Usage
All commands support `--json` flag for machine-readable output.
